<!--
  这份文件是 GitHub Release 正文的发版说明部分，由 .github/workflows/release.yml 读取。
  文末的「文档」章节是 workflow 自动拼上去的（链接要指向具体 tag，不能写死在这里），
  所以这里只写发版说明本身。

  注意正文格式：
     - **不要以 `# transgo x.y.z` 开头**。Release 页面的标题已经是版本号了，再写一遍是重复。
     - **不要写概括性的开场白**。读者点进 Release 就是来看改动细节的，直接从 `## 新增` 开始。

  发版流程：
    1. 重写本文件，写清这次的新增功能和改动
    2. git commit
    3. git tag v<版本号>        （要以 v 开头，例如 v0.1.0）
    4. git push --follow-tags

  workflow 会编译 x86_64 musl 静态二进制、打包、算校验和，然后建 Release 并把产物挂上去。
  同一份 workflow 能一直用下去，发新版不需要改它。
-->

## 新增

- 图形界面（M2）：`transgo gui` 打开独立翻译窗口，`transgo gui --clip` 启动时读一次剪贴板
  预填并直接翻一次。下拉换引擎和语向，回车翻译、Shift + 回车换行，译文一键复制。
  浅色界面，原文标题行实时显示字符数——计费按源字符算，看得见
- 翻译在后台线程执行，长文本不卡界面；中文字体从系统自动挂载，找不到会显示方框，
  装个 noto-fonts-cjk 即可
- 还是那个单文件：GUI 直接编译进同一个二进制，musl 静态构建不变，部署方式与之前完全一样

## 修复

- 选项写在文本之后会被吞成待翻译内容（`transgo "hello" -v` 曾译出 `你好-v`）。
  现在选项在任意位置都生效；以 `-` 开头的文本放到 `--` 之后，裸 `-` 仍是 stdin 标记
